fn squares(nums: &[i32]) -> Vec<i32> { // TODO: Return the square of each number.
}
fn main() {}
#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn maps_values() {
        assert_eq!(squares(&[1, 2, 3]), [1, 4, 9]);
    }
}
